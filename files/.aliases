alias s='ssh dev'
alias m='mosh dev'
alias t='tmux new-session -AD -s'
alias st='ssh -A dev -t tmux new-session -AD -s'
alias mt='mosh dev -- tmux new-session -AD -s'
alias sync-insights='sudo lsyncd /etc/lsyncd/insights.conf.lua'
alias sync-dashboard='sudo lsyncd /etc/lsyncd/dashboard.conf.lua'

# temp
alias sa='ssh dev -t "cd ~/analytics ; bash"'
alias sb='ssh dev -t "cd ~/analytics/billing ; bash"'
alias si='ssh dev -t "cd ~/platform-apps/insights/src ; bash"'

# Git aliases
alias gs='git status'
alias gst='git stash'
alias gsl='git stash list'
function gss {
  if [[ $1 ]]; then
    git stash show -p stash@{$1}
  else
    git stash show -p
  fi
}
function gsa {
  if [[ $1 ]]; then
    git stash apply stash@{$1}
  else
    git stash apply
  fi
}
function gsp {
  if [[ $1 ]]; then
    git stash pop stash@{$1}
  else
    git stash pop
  fi
}
alias ga='git add'
alias gaa='git add .'
alias gc='git commit'
alias gcm='git commit -m'
alias gca='git commit --amend'
alias gp='git push'
alias gb='git branch'
alias gbd='git branch -D'
alias gco='git checkout'
alias gcb='git checkout -b'
alias gpl='git pull'
alias gps='git push'
alias gm='git merge'
alias gf='git fetch'
alias gl="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias gd='git diff'
alias gdc='git diff --cached'
alias gcp='git cherry-pick'
alias gr='git rebase'
alias grm='git rebase master'
alias grc='git rebase --continue'
alias gra='git rebase --abort'
alias grh='git reset HEAD'
alias gundo='git reset --soft HEAD~1 && git reset' # undo last commit and unstage changes
